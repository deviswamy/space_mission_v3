// Typed supertest wrapper — use in integration tests instead of raw fetch.

import { app } from "../../server/app";

// TODO: import supertest when added to devDependencies
// import supertest from "supertest";
// export const request = supertest(app);

// Placeholder until supertest is wired up
export { app };
